import { Metadata } from "next/types";

export const metadata: Metadata = {
  title: "Income expenses page",
  description: "Income expenses form",
};

export default function IncomeExpensesPage() {
  return (
    <div>
      <span className="text-gray-500">IncomeExpensesPage</span>
    </div>
  );
}